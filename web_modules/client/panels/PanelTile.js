"use strict";

/**
 * A panel tile represents a tile within the static space for panels.
 * A panel tile basically is a docking point for panels.
 *
 * Panel tiles can be split into multiple panel tiles.  When this happens the panel tile's 
 * children list will be expanded.  At most the children list can be 2?.
 */
class PanelTile {
    constructor() {

    }
}

module.exports = PanelTile;